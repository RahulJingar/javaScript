
const orders = [{ id: 1, status: 'pending' },  

{ id: 2, status: 'delivered' }];

const order = orders.filter(o => o.status == 'delivered');
console.log(order);

//Output:- [{ id: 2, status: 'delivered' }]
