/*30. Frequency of Array Elements
Example: [1,2,2,3,3,3] → {1:1,2:2,3:3}.*/

let arr = [1,2,2,3,3,3];
let freq = {};

for(i=0;i<arr.length;i++){
    if(freq[arr[i]]){
        freq[arr[i]] += 1;
    }else{
        freq[arr[i]] = 1;
    }
}

console.log(freq);
