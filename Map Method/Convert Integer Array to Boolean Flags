
const flags = [0, 1, 0]

const booleanFlags = flags.map(f => {
    if(f == 0){
        return false;
    }
    else if(f == 1)
    {
        return true;
    }
});

console.log(booleanFlags);

//Output:- [ false, true, false ]
